function generateHtmlPage() {
    function renderCard(employee) {
        switch (employee) {
            case 'manager':
                return ``;
            case 'Engineer':
                return ``;
            case 'Intern':
                return ``;
        }
    };
};

module.exports = generateHtmlPage;